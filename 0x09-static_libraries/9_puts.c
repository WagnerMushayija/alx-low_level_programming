#include "main.h"
#include <stdio.h>
/**
 * _puts - prints string with new linw after
 * @str: string to be prited with newline
 * Return: 0 if successs
 */
void _puts(char *str)
{
	printf("%s\n", str);
}
